/*
Crie um programa que receba uma frase e substitua todas as letras “a” por “e”.
*/

const frase = 'Javascript é mamão com açúcar!';

const fraseConvertida = frase.replaceAll('a','e');
const fraseConvertida2 = fraseConvertida.replaceAll('ã','e');

console.log(fraseConvertida2);